var hours = process.argv[2];
var minutes = process.argv[3];
If (minutes >= 60) {
  alert ('введите корректное время (минуты)');}
else If (hours > 24) {
  alert ('введите корректное время (часы)');}

// Немного замечательного кода и магии
